# Platform database (shared across all tenants)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/saas_platform?schema=public"

# Example tenant database URL (dynamic in production)
TENANT_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tenant_demo?schema=public"